// TODO: implement in Task 5 — rehype-sanitize schema (extends defaultSchema
// with the five MDX allowlist tags, <pre>/<code>, <sub>/<sup>; strips
// <script>, raw <iframe>, inline event handlers, and style attributes).
export {};
