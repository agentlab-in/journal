'use client'

/**
 * Phase 6 placeholder. Real follow behaviour lands in Phase 8.
 * Renders a disabled button so the spec slot is occupied.
 */
export function FollowButton() {
  return (
    <button
      type="button"
      className="follow-stub"
      disabled
      aria-disabled
      title="Coming soon"
    >
      Follow
    </button>
  )
}
